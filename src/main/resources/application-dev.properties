# Configuracion para DESARROLLO (local)
# Base de datos PostgreSQL para desarrollo (usando tu configuracion que funcionaba)
spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/siq3
spring.datasource.username=postgres
spring.datasource.password=xxlopesxx12
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none

# Email Configuration para desarrollo (usa valores dummy)
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=dev@localhost
spring.mail.password=devpassword

# Logging basico para desarrollo
logging.level.com.example.fleetIq=DEBUG
