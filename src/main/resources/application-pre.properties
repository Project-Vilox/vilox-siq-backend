# Configuracion para PREPRODUCCION
# Base de datos PostgreSQL (usando variables de entorno)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d3gb1015pdvs73ebg360-a.oregon-postgres.render.com:5432/vilox_db_cz40}
spring.datasource.username=${DATABASE_USERNAME:vilox_db_cz40_user}
spring.datasource.password=${DATABASE_PASSWORD:dSjKnkQn8gKLKTe1MThbxrBqDeGsxhNU}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Server configuration
server.port=${PORT:8080}

# JWT usando variables de entorno
jwt.secret=${JWT_SECRET:mi_clave_secreta_super_segura_para_jwt_que_debe_tener_al_menos_32_caracteres_para_ser_segura}

# Email configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Frontend URL
app.frontend.url=${FRONTEND_URL:http://localhost:3000}

# Logging para preproduccion
logging.level.root=INFO
logging.level.com.example.fleetIq=INFO
